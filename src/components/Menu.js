import styled from "styled-components"

const Wrapper = styled.div`
  height: 100vh;
  min-width: 10%;
  padding: 20px;
  background-color: red;
`

const Menu = () => {
  return(
    <Wrapper></Wrapper>
  )
}

export default Menu